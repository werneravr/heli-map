# AI Agent Guidelines for TMNP Helicopter Tracking System

This file provides critical guidelines for AI coding assistants (Cursor, Warp, GitHub Copilot, etc.) working on this project. **Please read and follow these rules carefully to maintain project organization and consistency.**

## 📂 File Organization Rules

### Where to Create New Files

#### **Production Scripts** → `/backend/scripts/`
Scripts that are part of the core workflow and production system.

**Examples:**
- `generate-flight-image.cjs` - Creates PNG violation maps
- `smart-kml-manager.cjs` - KML processing and duplicate detection
- `build-static-site.cjs` - Static site generation
- `optimise_kml.py` - KML optimization

**When to use:**
- Scripts that run as part of the normal admin workflow
- Scripts referenced by other production code
- Scripts executed by the admin interface
- Any script that processes real production data

#### **Test/Debug Scripts** → `/backend/scripts/tests/`
Temporary debugging scripts and test utilities.

**Examples:**
- `test-violation.cjs` - Tests violation detection logic
- `debug-tmnp.cjs` - Debugs TMNP boundary loading
- `analyze-specific-file.cjs` - Analyzes specific KML files

**When to use:**
- Quick debugging scripts for troubleshooting issues
- One-off analysis scripts
- Scripts that test specific functionality
- Temporary scripts that won't be part of production workflow

**Important:** These scripts should be prefixed with `test-` or `debug-` for clarity.

#### **Configuration Files** → `/backend/config/`
System configuration and state tracking files.

**Examples:**
- `deployment-config.json` - GitHub deployment settings
- `deployment-status.json` - Current deployment state

**When to use:**
- Application configuration files (JSON, YAML, etc.)
- State tracking files that persist between runs
- Settings that users might customize

#### **Metadata/Runtime Data** → `/backend/scripts/`
Runtime data, flight metadata, and cached information.

**Examples:**
- `master-metadata.json` - Flight database
- `helicopters.json` - Aircraft information
- `processed-files.json` - Tracking of processed files
- `duplicates/` - Duplicate flight files
- `images/` - Helicopter photos
- `hot-reload.sh` - Development utility

**When to use:**
- Flight data and metadata
- Cached information
- Runtime state that's generated by the system

**Important:** `/backend/server/` folder is DEPRECATED. All metadata now lives in `/backend/scripts/`

#### **Log Files** → `/backend/logs/`
All log files generated by scripts, servers, or processes.

**Examples:**
- `server.log` - Main backend server logs
- `static-site-server.log` - Static site server logs
- `deployment.log` - Deployment system logs
- `optimize-kml.log` - KML optimization process logs

**When to use:**
- Any script that generates logs
- Shell redirects (`> logs/filename.log`)
- Application logging output

**Important:**
- NEVER create log files in backend root or project root
- ALL logs must go in `/backend/logs/`
- Log files are auto-generated and gitignored
- Use descriptive names: `[process-name].log`

### Documentation Files → Project Root (`/`)

**All `.md` documentation files belong in the project root**, not in subdirectories (except `/static-site/README.md` which is part of the deployed site).

**Main documentation:**
- `README.md` - **Single comprehensive documentation file** containing:
  - Project overview and purpose
  - Quick start and setup
  - Architecture and file organization
  - AI agent guidelines
  - Deployment system documentation
  - Terminal commands and troubleshooting

**Why:** Consolidated single-source-of-truth documentation is easier to maintain and ensures consistency.

### ❌ DO NOT Create Files In These Locations:

1. **Project Root** (`/`) - Reserved ONLY for:
   - Documentation files (`.md`)
   - Configuration files (`.gitignore`, `.env`, etc.)
   - `.ai/` directory - AI agent guidelines
   - `launch.sh` - Main system launcher (starts all servers)
   - `package.json` - Only if this becomes a root-level Node project
   - **NO processing scripts, NO test scripts, NO random files**

2. **Backend Root** (`/backend/`) - Reserved for:
   - `backend.html` - Main admin interface
   - `launch-backend-only.sh` - Backend-only launcher (advanced)
   - `package.json`, `package-lock.json` - Node.js dependencies
   - Core directories: `uploads/`, `flight-maps/`, `scripts/`, `server/`, `config/`, `logs/`
   - **NO log files** - All logs go in `/backend/logs/`

**Note:** The main unified launcher (`launch.sh`) is in the project root and starts both servers.

3. **Static Site** (`/static-site/`) - This is a GENERATED directory:
   - Files here are created by build scripts
   - Never manually edit or create files here
   - The static site is built from backend data
   - **CRITICAL**: PNG flight maps NEVER go here - they stay in `/backend/flight-maps/` and are referenced via GitHub URLs

## 🏗️ Project Architecture Principles

### Backend vs Static Site Separation

**Critical Rule:** The `/static-site/` directory NEVER references anything in `/backend/`.

#### ✅ Static Site CAN Reference:
- Files within `/static-site/` directory itself
- Images and KMLs served from GitHub raw URLs
- External CDN resources

#### ❌ Static Site CANNOT Reference:
- Any files in `/backend/uploads/`
- Any files in `/backend/flight-maps/`
- Local backend server endpoints
- Relative paths pointing to backend folders

### Why This Matters:
- The static site is hosted on cheap cloud platforms (GitHub Pages, Netlify)
- The backend runs locally for data processing
- Mixing references breaks the deployment model

## 📝 Naming Conventions

### Scripts:
- **Production scripts**: `[action]-[noun].cjs` or `[verb]-[noun].cjs`
  - Examples: `generate-flight-image.cjs`, `build-static-site.cjs`, `smart-kml-manager.cjs`
- **Test scripts**: `test-[feature].cjs`
  - Examples: `test-violation.cjs`, `test-duplicate-detection.cjs`
- **Debug scripts**: `debug-[feature].cjs`
  - Examples: `debug-tmnp.cjs`, `debug-coordinates.cjs`

### Configuration Files:
- Use descriptive names: `deployment-config.json`, `helicopters.json`
- Use JSON for configuration (not YAML or other formats, unless necessary)

### Data Files:
- KML files: `[AIRCRAFT-REG]_[DATE]_violation.kml`
  - Example: `ZS-HBO_2024-03-15_violation.kml`
- PNG files: `[AIRCRAFT-REG]_[DATE]_violation.png`
  - Example: `ZS-HBO_2024-03-15_violation.png`

## 🔍 Before Creating a New File, Ask:

1. **Is this a documentation file (`.md`)?**
   - Yes → Project root (`/`)
   - Exception: `/static-site/README.md` stays in static site

2. **Is this a log file (`.log`)?**
   - Yes → `/backend/logs/`
   - NEVER in backend root or project root

3. **Is this production code or temporary debug code?**
   - Production → `/backend/scripts/`
   - Temporary → `/backend/tests/debug/`

4. **Is this a configuration file?**
   - Yes → `/backend/config/`

5. **Is this runtime/metadata?**
   - Yes → `/backend/server/`

6. **Does this file need to be in the project root?**
   - Almost always NO (except for documentation and config files like `.gitignore`)

## 📊 Directory Structure Reference

```
heli-map/
├── launch.sh                     # 🚀 Unified launcher (starts both servers)
├── README.md                     # 📖 Single comprehensive documentation
├── .gitignore                    # Git ignore rules
├── .ai/                          # AI agent guidelines
│   └── guidelines                # File organization rules (this file)
│
├── static-site/                  # 🌐 PUBLIC WEBSITE (generated, hosted)
│   ├── index.html                # Main website interface
│   ├── README.md                 # Static site deployment guide
│   ├── tmnp.kml                  # TMNP boundary file
│   ├── kml-optimised/           # Optimized KML files (public)
│   └── NEMPAA.pdf, NP17.pdf     # Documentation PDFs
│
└── backend/                     # 🔧 DATA PROCESSING (local)
    ├── backend.html              # Admin web interface
    ├── launch-backend-only.sh    # Backend-only launcher (advanced)
    ├── package.json              # Node.js dependencies
    │
    ├── config/                   # ⚙️ Configuration files
    │   ├── deployment-config.json
    │   └── deployment-status.json
    │
    ├── logs/                     # 📋 Log files (all logs go here!)
    │   ├── server.log            # Main backend server logs
    │   ├── static-site-server.log # Static site server logs
    │   ├── deployment.log        # Deployment system logs
    │   └── optimize-kml.log      # KML optimization logs
    │
    ├── scripts/                  # 🚀 Production scripts & metadata
    │   ├── index-no-auth.cjs     # Main backend server
    │   ├── smart-kml-manager.cjs # KML processing
    │   ├── generate-flight-image.cjs # PNG generation
    │   ├── build-static-site.cjs # Static site builder
    │   ├── deploy-to-github.cjs  # Git deployment
    │   ├── master-metadata.json  # Flight database
    │   ├── helicopters.json      # Aircraft information (curated)
    │   ├── processed-files.json  # Processing tracking
    │   ├── duplicates/           # Duplicate flight files
    │   ├── images/               # Helicopter photos
    │   └── hot-reload.sh         # Development utility
    │
    ├── tests/                    # 🧪 Test & debug scripts
    │   └── debug/                # Debug scripts directory
    │       ├── test-*.cjs        # Test scripts
    │       └── debug-*.cjs       # Debug scripts
    │
    ├── uploads/                  # ✈️ Original KML files
    │   ├── ZS-HBO/              # Organized by aircraft
    │   ├── ZS-HIE/
    │   └── [other-aircraft]/
    │
    └── flight-maps/             # 🗺️ PNG violation screenshots
        ├── ZS-HBO/              # Organized by aircraft
        ├── ZS-HIE/
        └── [other-aircraft]/

Note: /backend/server/ folder is DEPRECATED - all metadata moved to /backend/scripts/
```

## 🔧 Path Reference for Scripts

When writing scripts in different locations, use these path patterns:

### From `/backend/scripts/` (production scripts):
```javascript
// Access TMNP boundary
const tmnpPath = path.join(__dirname, '..', '..', 'static-site', 'tmnp.kml');

// Access uploads
const uploadsDir = path.join(__dirname, '..', 'uploads');

// Access metadata (in same directory now)
const metadataPath = path.join(__dirname, 'master-metadata.json');
const helicoptersPath = path.join(__dirname, 'helicopters.json');

// Access config
const configPath = path.join(__dirname, '..', 'config', 'deployment-config.json');
```

### From `/backend/tests/debug/` (debug scripts):
```javascript
// Access TMNP boundary
const tmnpPath = path.join(__dirname, '..', '..', '..', 'static-site', 'tmnp.kml');

// Access uploads
const uploadsDir = path.join(__dirname, '..', '..', 'uploads');

// Access metadata (in scripts/ directory)
const metadataPath = path.join(__dirname, '..', '..', 'scripts', 'master-metadata.json');
const helicoptersPath = path.join(__dirname, '..', '..', 'scripts', 'helicopters.json');

// Access config
const configPath = path.join(__dirname, '..', '..', 'config', 'deployment-config.json');
```

## 🚨 Common Mistakes to Avoid

1. **Creating test scripts in `/backend/` root** → Use `/backend/scripts/tests/` instead
2. **Creating config files in `/backend/` root** → Use `/backend/config/` instead
3. **Hardcoding paths without `__dirname`** → Always use `path.join(__dirname, ...)` for portability
4. **Making static site reference backend files** → Static site must be self-contained
5. **Creating files in project root** → Almost always wrong, use a subdirectory
6. **Not using proper naming conventions** → Follow `test-`, `debug-`, or `action-noun` patterns
7. **Editing generated files in `/static-site/`** → These are built by scripts, edit the source

## 📖 Additional Resources

- **Full Documentation**: See `/README.md` - comprehensive single-source-of-truth documentation containing:
  - Project overview and architecture
  - AI agent guidelines and terminal commands
  - Deployment system documentation
  - Complete workflow and troubleshooting guides

## 🤝 Contributing New Features

When adding new functionality:

1. **Plan the file location** using the rules above
2. **Use descriptive names** following the naming conventions
3. **Update documentation** if adding new workflows or concepts
4. **Test thoroughly** before committing
5. **Keep production and debug code separate**

---

**Remember:** These guidelines exist to keep the project organized and maintainable. When in doubt, ask yourself: "Is this production code, test code, configuration, or data?" That will guide you to the right location.

